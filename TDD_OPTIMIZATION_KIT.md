# TDD Optimization Kit for AI-Driven Development

## 1. 導入手順
このキットは、AIエージェント（Claude Code, Codex, Sol等）の暴走を防ぎ、開発効率を最大化するために設計されています。

1.  `AGENTS.md` (または `CLAUDE.md`) をプロジェクトルートに配置します。
2.  `GUARDRAILS.md` を配置し、プロジェクト固有の禁止事項を追記します。
3.  エージェントに対し、「指示ファイルを読み込み、それに従って行動せよ」と一度伝えます。

## 2. 推奨されるワークフロー（AIへのプロンプト例）

### 機能実装時
> "〇〇機能を実装して。まずは最小限の単体テストを1つ作成し、それが通ることを確認してから実装を進めて。NFR（負荷・セキュリティ等）の検証は現時点では不要です。"

### テスト失敗時
> "テストが失敗した原因を分析して。コードの修正案を出す前に、原因が『テストコードの不備』なのか『実装のバグ』なのかを切り分けて報告して。"

## 3. リソース節約のテクニック

| 項目 | 対策 | メリット |
| :--- | :--- | :--- |
| **テストの絞り込み** | `--grep` やファイル指定で対象を最小化する。 | 実行時間短縮、トークン節約 |
| **モックの活用** | 外部通信をすべてモック化する。 | フラッキーなエラーの排除 |
| **逐次実行** | `AGENTS.md` で並列実行を制限する。 | CPU負荷の低減、熱暴走防止 |

## 4. チェックリスト
- [ ] 不要な指示（「丁寧に」「正確に」等の抽象的表現）を削除したか？
- [ ] リトライ回数の上限が明示されているか？
- [ ] 終了時にプロセスをKillする指示が含まれているか？
- [ ] 人間の介入が必要な「境界条件」が定義されているか？
